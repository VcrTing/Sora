准备:
yum install -y git
yum install -y vim
cd ~
mv Sora OLDER_Sora
git clone 'https://www.github.com/VcrTing/Sora.git'
cd /root/Sora/doc/install_init/Install_Shell
ls

之后:
  sh + 文件名.sh
  # 执行一步到位安装程序

  yaml 文件中的命令 则需要手动执行

之后:
  # cd 到项目/Install 文件夹下面
  cd /root/SMSTask/CLONE
  # 执行 针对项目 的安装
  sh 8_install.sh