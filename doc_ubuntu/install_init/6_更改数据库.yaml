mysql -u root -p
aaa

use mysql;

update mysql.user set authentication_string=password('VcrTing.ZT123zlt') where user='root' and Host = 'localhost';

update user set plugin='mysql_native_password';

CREATE DATABASE soul DEFAULT CHARACTER SET utf8 COLLATE utf8_general_ci;

exit;